# ربط موقع "دكان" بـ Firebase (مجاني بالكامل)

الموقع دلوقتي بقى فيه تسجيل دخول حقيقي، وحفظ منتجات فعلي في قاعدة بيانات، وحفظ صور حقيقي.
كل اللي محتاجه إنك تعمل مشروع Firebase مجاني وتحط بياناته في مكان واحد. الخطوات دي هتاخد منك حوالي 10 دقايق.

**ملحوظة:** الموقع بيخزّن صور المنتجات كـ base64 جوه Firestore مباشرة (بعد ما بيصغّرها ويضغطها في المتصفح تلقائياً)، **مش** عن طريق Firebase Storage. ده معناه إنك مش محتاج تفعّل خطة Blaze (المدفوعة) خالص، وهتقدر تنشر الموقع للجمهور مجاناً بالكامل على خطة Spark المجانية.

---

## 1) أنشئ مشروع Firebase

1. روح على https://console.firebase.google.com
2. سجّل دخول بحساب جوجل
3. دوس **Add project / إضافة مشروع**
4. اكتب اسم زي `dokan-app` ودوس **Continue**
5. تقدر تسيب خاصية Google Analytics **متعطلة (Disabled)** عشان تبسيط الإعداد، ثم **Create project**

---

## 2) فعّل تسجيل الدخول (Authentication)

1. من القائمة الجانبية: **Build > Authentication**
2. دوس **Get started**
3. من تبويب **Sign-in method**، فعّل:
   - **Email/Password** (لتسجيل الدخول بالإيميل وكلمة المرور)
   - **Google** (لزرار "الدخول عبر جوجل") — هيطلب منك إيميل دعم، اختار إيميلك

---

## 3) فعّل قاعدة البيانات (Firestore Database)

1. من القائمة الجانبية: **Build > Firestore Database**
2. دوس **Create database**
3. اختار موقع قريب منك (مثلاً `eur3 (europe-west)`)
4. ابدأ بوضع **Start in production mode** (هنحط القواعد الصح بعدين في خطوة 5)

---

## 4) اربط الموقع بالمشروع (خطوة النسخ واللصق)

> ملحوظة: مفيش خطوة "فعّل Storage" هنا خالص — الموقع مش محتاجها، لأن الصور بتتخزن جوه Firestore نفسه، فتقدر تفضل على خطة Spark المجانية طول الوقت.

1. من صفحة المشروع الرئيسية، دوس أيقونة **</> (Web)** لإضافة تطبيق ويب
2. اكتب اسم مستعار (مثلاً `dokan-web`) ودوس **Register app**
3. هتظهر لك بيانات زي دي:
   ```js
   const firebaseConfig = {
     apiKey: "AIza...",
     authDomain: "dokan-app.firebaseapp.com",
     projectId: "dokan-app",
     storageBucket: "dokan-app.appspot.com",
     messagingSenderId: "123456789",
     appId: "1:123456789:web:abcdef123456"
   };
   ```
4. افتح ملف **`firebase-config.js`** في مجلد الموقع، واستبدل القيم الموجودة فيه بالقيم دي بالظبط.

---

## 5) حط قواعد الأمان (مهم جداً قبل ما توفتح الموقع للناس)

### Firestore
1. **Build > Firestore Database > Rules**
2. امسح اللي موجود، وانسخ فيه محتوى ملف **`firestore.rules`** المرفق
3. دوس **Publish**

القواعد دي بتضمن إن:
- أي حد يقدر يتصفح المنتجات والصور من غير تسجيل دخول
- بس نشر منتج يتطلب تسجيل دخول، وعدد صوره متحكم فيه (لغاية 4) عشان مستند المنتج ميكبرش عن حد Firestore
- محدش يقدر يعدّل أو يمسح منتج غير صاحبه

> ملف **`storage.rules`** المرفق سايبينه للمستقبل بس مش هتحتاجه دلوقتي، لأن الموقع مش بيستخدم Firebase Storage أصلاً.

---

## 6) شغّل الموقع

الموقع عبارة عن ملفات HTML عادية، بس لازم تفتحه عن طريق سيرفر محلي بسيط (مش بفتح الملف مباشرة من الجهاز)، عشان الـ ES Modules (`import`) تشتغل صح في المتصفح.

أسهل طريقة لو عندك Python مثبت على جهازك:
```bash
cd مجلد-الموقع
python3 -m http.server 8000
```
وبعدين افتح المتصفح على: `http://localhost:8000/index.html`

بديل تاني: امتداد **Live Server** في VS Code، أو أي استضافة زي **Firebase Hosting** نفسها (مجانية كمان، ونقدر نظبطها في خطوة تالية لو حبيت).

---

## 7) جرّب بنفسك

1. افتح `auth.html` واعمل حساب جديد ببريدك
2. هتلاقي نفسك رجعت لـ `index.html` وأيقونة الحساب اتغيرت لحرف اسمك
3. دوس **"أضف منتجك"**، املأ البيانات وارفع صورة، وانشر
4. المنتج هيظهر فعلياً في **Firestore Database** (مجموعة `products`) وفي صفحة **"كل المنتجات"** و**الصفحة الرئيسية**
5. الصور اللي رفعتها هتلاقيها متخزنة كـ base64 جوه مستند المنتج نفسه في Firestore (مش في Storage)

---

## 8) انشره فعلياً للجمهور (Firebase Hosting - مجاني بالكامل)

ده آخر خطوة، وبعدها هيبقى عندك رابط عام حقيقي زي `https://dokan-64545.web.app` أي حد في الدنيا يقدر يفتحه. خطة Spark المجانية فيها 10GB تخزين و360MB نقل بيانات يومياً، أكتر من كفاية لموقع بيبدأ، ومفيش أي بطاقة بنكية مطلوبة.

### أ) ثبّت أداة Firebase على جهازك
لازم يكون عندك **Node.js** مثبت الأول (لو مش عارف، حمّله من https://nodejs.org). بعدين افتح Terminal / موجه الأوامر واكتب:
```bash
npm install -g firebase-tools
```

### ب) سجّل دخول من الأداة
```bash
firebase login
```
هيفتحلك المتصفح تسجّل دخول بنفس حساب جوجل اللي عملت بيه المشروع.

### ج) جهّز المشروع للنشر
من جوه مجلد الموقع (اللي فيه `index.html` وكل الملفات):
```bash
cd مجلد-الموقع
firebase init hosting
```
هيسألك كام سؤال، جاوب كده:
- **"Please select an option"** → اختار **Use an existing project** وحدد `dokan-64545`
- **"What do you want to use as your public directory?"** → اكتب `.` (نقطة واحدة، يعني المجلد الحالي نفسه)
- **"Configure as a single-page app (rewrite all urls to /index.html)?"** → اختار **No** (لأن الموقع فيه صفحات منفصلة زي `search.html`، `product.html`... مش صفحة واحدة)
- **"Set up automatic builds and deploys with GitHub?"** → اختار **No** (تقدر تفعّلها بعدين لو حبيت)
- لو سألك يعمل overwrite لـ `index.html`، اختار **No** عشان ميمسحش الملف بتاعك

### د) انشر
```bash
firebase deploy --only hosting
```
في الآخر هتلاقي رسالة فيها **Hosting URL** — ده رابط موقعك الشغال فعلياً للجمهور دلوقتي. أي تحديث بعد كده، كرر نفس أمر `firebase deploy --only hosting` وبس.

### ملاحظة عن تسجيل الدخول بجوجل
لو بتستخدم "الدخول عبر جوجل"، دومين الاستضافة الافتراضي (`*.web.app` و`*.firebaseapp.com`) بيبقى مُصرّح له تلقائياً. لو ربطت دومين خاص بيك بعدين، هتحتاج تضيفه يدوي من **Authentication > Settings > Authorized domains**.

---

## ملاحظات مهمة

- **مفيش أي مفتاح سري في الكود**: القيم اللي في `firebase-config.js` (زي `apiKey`) دي عادي تكون ظاهرة في كود الموقع، الحماية الحقيقية بتيجي من قواعد الأمان (`firestore.rules`) مش من إخفاء المفتاح ده.
- كل صفحات الموقع بقت بتستورد نفس ملفين: `firebase-config.js` (الإعدادات) و`auth-header.js` (حالة تسجيل الدخول في الهيدر)، فلو غيرت بيانات المشروع، غيرها في `firebase-config.js` بس وهتنعكس على كل الصفحات.
- الصور بتتصغّر وتتضغط تلقائياً في المتصفح (لحد 900px وجودة JPEG متغيرة) قبل ما تتبعت لـ Firestore، عشان تفضل خفيفة وميحصلش تعدي لحد المستند الواحد (1 ميجا). لو حابب صور بجودة أعلى في المستقبل، هنحتاج وقتها نرجع لـ Firebase Storage (ومعاه خطة Blaze).
- زرار "تواصل مع البائع" في صفحة المنتج بيفتح واتساب مباشرة برقم البائع الحقيقي المسجل وقت النشر.
- خاصية "المفضلة" لسه شكلية بصرياً بس مش محفوظة في قاعدة البيانات — دي خطوة تانية ممكن نضيفها بعدين لو حبيت.
